@model IEnumerable<AirportProj.Models.Airline>

@{
    ViewData["Title"] = "Index";
}

<header>
    <nav class="navbar fixed-top navbar-expand-sm navbar-toggleable-sm navbar-custom border-bottom box-shadow">
        @Html.Partial("AdminNavigation")
    </nav>
</header>
<h1>Інформація</h1>
<hr />
<div class="jumbotron">
    <p>
        <a asp-action="Create">Новий запис</a>
    </p>
    <hr />
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IataCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IcaoCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IataCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IcaoCode)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AirlineId">Редагувати</a> |
                <a asp-action="Details" asp-route-id="@item.AirlineId">Детальніше</a> |
                <a asp-action="Delete" asp-route-id="@item.AirlineId">Видалити</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>